name: Vercel Deployment Notice

on:
  workflow_dispatch:

jobs:
  deployment-notice:
    runs-on: ubuntu-latest

    steps:
      - name: Explain deployment owner
        run: |
          echo "Production deploys are handled by the Vercel Git integration."
          echo "Pushing to main creates a Vercel deployment for ai.starlab.co.kr."
